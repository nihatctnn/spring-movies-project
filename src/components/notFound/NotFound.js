// Import the React library to use its features
import React from 'react';

// Define a functional component named NotFound
const NotFound = () => {
  return (
    // Return a JSX element, in this case, a <div> (HTML div element)
    <div>
      // Display a message indicating that the page could not be found
      This page could not be found
    </div>
  );
}

// Export the NotFound component to make it available for use in other parts of the application
export default NotFound;
